# eyego-internship-task2
# To run the application please follow the following instructions:
# 1-npm install 2-docker-compose up -d 3-docker ps 4-cp .env.ex .env 5-npm run dev  and then in a seperate terminal run 'npm run start:consumer'

